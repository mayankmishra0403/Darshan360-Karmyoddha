Place the Gwalior Fort image here

To use the local Gwalior Fort image in the site, save the image you provided (from the attachments) as:

  `public/images/gwalior-fort.jpg`

Notes:
- The project `src/data/content.ts` was updated to reference `/images/gwalior-fort.jpg` for the Gwalior hero and gallery images.
- After placing the image file, restart the Next.js dev server if it's running.

If you prefer a different filename or location, update the `heroImage` and `gallery.src` values in `src/data/content.ts` accordingly.
